# Video Collector v3 (Telethon + Aiogram)

Крутой “боевой” бот с логированием и понятным прогрессом.

## Что умеет
- Сканирует личные чаты, группы, супергруппы и каналы аккаунта (через MTProto Telethon), но пропускает Избранное, `Подборки 18+`, чаты из `EXCLUDED_CHAT_IDS`, целевого бота отправки и каналы/группы, название которых начинается с `Vertical`.
- Ищет подходящие видео, **скачивает их во временную папку** и отправляет файлом в @Content_Vertical_BOT.
- По каждому выбранному чату проходит без лимита количества сообщений; режимы периода (`day`/`week`/`month`) всё равно ограничивают поиск датой.
- Управление через control-bot (Bot API): нижнее меню Telegram, выбор любых доступных чатов, быстрый выбор всей страницы, обновление списка, период, порядок, статус процесса и стоп.
- Показ прогресса: сколько чатов обработано, сколько сообщений проверено, сколько видео найдено/подошло и сколько отправлено.
- Логи в консоль и файл `logs/app.log` (ротируются).
- Отчёт после сканирования показывает причины отсева видео и топ чатов по найденным роликам.


## Режимы сканирования
- `new` — только сообщения новее последней сохранённой позиции по каждому чату.
- `day` — сообщения за последние 24 часа.
- `week` — сообщения за последние 7 дней.
- `month` — сообщения за последние 30 дней.
- `all` — вся история доступных чатов. Только этот режим для всех чатов обновляет список “пустых” чатов.

## Новые удобства меню
- Главное меню показано как нижняя клавиатура Telegram: на смартфоне её можно скрыть обычной стрелкой «назад», без отдельной кнопки скрытия.
- В выборе чатов есть кнопки “Выбрать страницу”, “Снять страницу” и “Обновить список”.
- Доступны команды `/start`, `/status` и `/help` для быстрого доступа без кнопок.
- Отчёт показывает найденные видео, причины отсева (`не вертикальные`, `короткие`, `узкие`, `маленький размер`) и топ чатов по результативности.
- Действия удаления/выхода из пустых каналов можно безопасно проверить с `DRY_RUN_DELETE=true`.

## Фильтр видео
- Вертикальное: h > w
- Длительность: >= 180 секунд
- Ширина: w >= 900
- Размер: >= 10 МБ на минуту (пропорционально)

## Windows PowerShell
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
copy .env.example .env
python main.py
```

## Конфигурация
- `DOWNLOADS_DIR` — временная папка для скачивания роликов перед отправкой; по умолчанию `runtime/downloads`. После успешной или неуспешной отправки временный файл удаляется.
- `DOWNLOADS_RESERVE_MB` — запас свободного места, который бот оставляет сверх размера конкретного ролика; по умолчанию `256`. Это убирает жёсткую проверку “нужно минимум 2 ГБ” и позволяет отправлять небольшие ролики даже на диске с ограниченным свободным местом.

Скопируй `.env.example` в `.env` и заполни токены/ключи. Локальный `.env`, сессии, базы и логи добавлены в `.gitignore`, чтобы не утекали секреты и runtime-состояние.

## Логи
- `logs/app.log`
- смотреть “хвост”:
```powershell
Get-Content .\logs\app.log -Tail 200 -Wait
```

## Важно
Перед первым запуском открой диалог с @Content_Vertical_BOT и нажми Start (чтобы отправка файлов была разрешена).

Если в отчёте появляется ошибка `Недостаточно свободного места в DOWNLOADS_DIR`, освободи место, перенеси `DOWNLOADS_DIR` на диск побольше или уменьши `DOWNLOADS_RESERVE_MB`. Бот также автоматически удаляет старые временные папки `video_*`, оставшиеся после аварийного завершения, если они старше 6 часов.
